{
  "outputDirectory": "dist",
  "buildCommand": "npm install --prefix ../.. && npx vite build",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
